import { PlaygroundView } from '@/components/playground-view';

export const metadata = { title: 'Code Playground. MeritFlow' };

export default function PlaygroundPage() {
  return <PlaygroundView />;
}
